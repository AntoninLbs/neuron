# ===========================================
# DATABASE (Supabase)
# ===========================================
# Trouve ces URLs dans: Supabase Dashboard > Settings > Database > Connection string
DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-west-1.pooler.supabase.com:5432/postgres"

# ===========================================
# AUTH (NextAuth.js)
# ===========================================
# Génère avec: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here"

# URL de base de l'app (localhost en dev, ton domaine en prod)
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (optionnel)
# Crée dans: https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID="your-google-client-id"
AUTH_GOOGLE_SECRET="your-google-client-secret"

# ===========================================
# EMAIL (pour magic link)
# ===========================================
# Resend (recommandé): https://resend.com
EMAIL_FROM="noreply@yourdomain.com"
RESEND_API_KEY="re_xxxxxxxxxxxx"

# ===========================================
# OPENAI (pour génération de questions)
# ===========================================
OPENAI_API_KEY="sk-proj-xxxxxxxxxxxx"

# ===========================================
# APP CONFIG
# ===========================================
# Intervals pour le spaced repetition (en jours)
# Format: "1,1,3,5,8,13,21,34"
SPACED_REPETITION_INTERVALS="1,1,3,5,8,13,21,34"

# XP par action
XP_CORRECT_ANSWER=10
XP_REVIEW_COMPLETED=5
XP_DAILY_GOAL=50
XP_STREAK_BONUS=25
